/**
 * Configuração do Next.js. Por enquanto não precisamos de ajustes,
 * então deixamos o objeto vazio para o Next.js usar os valores
 * padrão dele.
 */
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
